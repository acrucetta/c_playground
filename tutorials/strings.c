#include <stdio.h>

void main() {
  char *s = "Hello";
  char s[] = "Hello";

  // In C a string is defined by a pointer to the first char in the string
  // A zero-valued byte that indicates the last char in the string
}
